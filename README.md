# Zimbar (arquivado)

Este repositório foi unificado no monorepo
[`zimbar-app`](https://github.com/pedeosimm-creator/zimbar-app), que reúne
desktop (`desktop/`) e mobile/PWA (`mobile/`) num só lugar.

Use o `zimbar-app` a partir de agora — este repo fica só como histórico.
