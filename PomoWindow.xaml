<Window x:Class="Zimbar.PomoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Zimbar Pomodoro"
        SizeToContent="WidthAndHeight"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        ShowActivated="False">
    <!-- Pílula flutuante: leve, sutil, só pra saber que ele está ali -->
    <Border x:Name="Pill"
            Style="{StaticResource Zimbar.Border.CosmicCard}"
            CornerRadius="999"
            Padding="14,7"
            Margin="14"
            Opacity="0.88"
            Cursor="Hand"
            MouseLeftButtonDown="Pill_MouseLeftButtonDown"
            MouseWheel="Pill_MouseWheel"
            ToolTip="clica pausa · arrasta move · Ctrl+arrasta ou roda do mouse muda o tamanho">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" ShadowDepth="0" Opacity="0.45" Color="#7B5CFF"/>
        </Border.Effect>
        <StackPanel Orientation="Horizontal">
            <Ellipse x:Name="PhaseDot" Width="7" Height="7" Fill="{DynamicResource Accent}"
                     VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBlock x:Name="TimerText" Text="25:00"
                       FontSize="15" FontFamily="Segoe UI" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextMain}" VerticalAlignment="Center"/>
        </StackPanel>
        <Border.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Pausar / retomar" Click="Toggle_Click"/>
                <MenuItem Header="Zerar" Click="Reset_Click"/>
                <MenuItem Header="Tamanho">
                    <MenuItem Header="pequeno" Click="Size_Click" Tag="0.8"/>
                    <MenuItem Header="médio" Click="Size_Click" Tag="1.0"/>
                    <MenuItem Header="grande" Click="Size_Click" Tag="1.4"/>
                </MenuItem>
                <Separator/>
                <MenuItem Header="Fechar pomodoro" Click="Close_Click"/>
            </ContextMenu>
        </Border.ContextMenu>
    </Border>
</Window>
