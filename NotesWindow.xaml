<Window x:Class="Zimbar.NotesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:primitives="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework"
        Title="ZimNotes"
        Icon="assets/ZimNotes.png"
        Width="440" Height="620"
        MinWidth="320" MinHeight="420"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="True" ResizeMode="NoResize"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        RenderOptions.BitmapScalingMode="HighQuality">

    <Grid Margin="14">
        <Border Style="{StaticResource Zimbar.Border.CosmicCard}">

            <Grid Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,12" Background="Transparent"
                           Cursor="SizeAll" MouseLeftButtonDown="Header_MouseDown"
                           ToolTip="arrasta aqui pra mover a janela">
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Style="{StaticResource PrimaryBtn}" Click="New_Click" Content="+ nota" Padding="12,5"/>
                        <Button Style="{StaticResource Chip}" Click="CloseWin_Click" Content="✕" Margin="7,0,0,0" Padding="10,5"/>
                    </StackPanel>
                    <TextBlock DockPanel.Dock="Left" Text="⠿" FontSize="16" Margin="0,0,10,0"
                               VerticalAlignment="Center" Foreground="{DynamicResource TextDone}"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="ZimNotes" FontSize="19"
                                   FontFamily="{DynamicResource Display}"
                                   Foreground="{DynamicResource TextMain}"/>
                        <StackPanel Orientation="Horizontal" Margin="0,1,0,0">
                            <TextBlock x:Name="CountText" Text="" FontSize="11"
                                       Foreground="{DynamicResource TextDim}"/>
                            <TextBlock x:Name="StatusText" Text="" FontSize="11"
                                       Foreground="{DynamicResource AccentSoft}"
                                       Margin="10,0,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </DockPanel>

                <TextBox x:Name="SearchBox"
                         Grid.Row="1"
                         Style="{StaticResource InlineAdd}"
                         TextChanged="SearchBox_TextChanged"
                         FontSize="12.5"
                         Margin="0,0,0,12"
                         ToolTip="buscar notas"/>

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="NotesListPanel"/>
                </ScrollViewer>
            </Grid>
        </Border>

        <primitives:Thumb x:Name="ResizeThumb"
                          Width="26" Height="26"
                          HorizontalAlignment="Right"
                          VerticalAlignment="Bottom"
                          Margin="0,0,4,4"
                          Cursor="SizeNWSE"
                          DragDelta="ResizeThumb_DragDelta">
            <primitives:Thumb.Template>
                <ControlTemplate TargetType="{x:Type primitives:Thumb}">
                    <Grid Opacity="0.7">
                        <Border Width="24" Height="24"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                Background="Transparent"/>
                        <Rectangle Width="12" Height="2.5"
                                   Fill="{DynamicResource Ink}"
                                   HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                   Margin="0,0,2,7"
                                   RenderTransformOrigin="0.5,0.5">
                            <Rectangle.RenderTransform>
                                <RotateTransform Angle="-45"/>
                            </Rectangle.RenderTransform>
                        </Rectangle>
                        <Rectangle Width="8" Height="2.5"
                                   Fill="{DynamicResource Ink}"
                                   HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                   Margin="0,0,2,3"
                                   RenderTransformOrigin="0.5,0.5">
                            <Rectangle.RenderTransform>
                                <RotateTransform Angle="-45"/>
                            </Rectangle.RenderTransform>
                        </Rectangle>
                    </Grid>
                </ControlTemplate>
            </primitives:Thumb.Template>
        </primitives:Thumb>
    </Grid>
</Window>
